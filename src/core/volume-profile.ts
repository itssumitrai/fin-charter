/**
 * Re-export from the canonical implementation in src/indicators/volume-profile.ts.
 * The duplicate that lived here has been removed to avoid divergence.
 */
export {
  computeVolumeProfile,
  type VolumeProfileBin,
  type VolumeProfileResult,
  type VolumeProfileOptions,
} from '../indicators/volume-profile';
